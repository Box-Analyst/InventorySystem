<Page
    x:Class="InventorySystem.Views.Admin.AdminView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlAcrylicWindowBrush}">

    <Grid>
        <StackPanel Canvas.Left="10" Canvas.Top="101">
            <TextBlock>Manage Samples</TextBlock>
            <TextBlock>Manage Database</TextBlock>
            <Button Click="Import_Database" Content="Import Database" />
            <Button Click="Export_Database" Content="Export Database" />
            <TextBox x:Name="InputBox"/>
            <Button Click="Add_Text" Content="Add" />
            <ListView x:Name="Output">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
